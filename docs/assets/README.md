# Documentation Assets

This directory stores hand-maintained documentation artwork used by public Markdown files.

- `readme-showcase.svg`: self-contained README showcase image. Keep it inline-SVG compatible with GitHub rendering; do not depend on external symbol files or generated build assets.

Runtime web assets that must be served by Vite belong in `public/`.
