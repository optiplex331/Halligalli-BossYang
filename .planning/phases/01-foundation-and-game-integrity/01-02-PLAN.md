---
phase: 01-foundation-and-game-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/App.jsx
  - src/game/
  - src/__tests__/
autonomous: true
requirements:
  - PER-01
  - QLT-02
user_setup: []
must_haves:
  truths:
    - "Malformed persisted settings and summaries no longer crash render-time lookups."
    - "Storage write failures degrade safely instead of breaking the session."
  artifacts:
    - path: "src/game/"
      provides: "Persistence normalization helpers or validation helpers used by the app"
    - path: "src/__tests__/"
      provides: "Regression coverage for malformed persisted input and persistence fallback behavior"
  key_links:
    - from: "src/App.jsx"
      to: "src/game/"
      proves: "The app normalizes persisted values before using them in render or summary code"
    - from: "src/__tests__/"
      to: "src/App.jsx"
      proves: "Known malformed persistence paths are covered by automated regression tests"
---

<objective>
Harden local persistence so stored data cannot break the current product loop.

Purpose: The app depends on browser-local settings and score summaries, so persistence must be normalized and fail-safe before later phases add more UI and data pathways.
Output: Persistence validation helpers, safer read/write integration in `src/App.jsx`, and regression coverage for malformed local data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-game-integrity/01-CONTEXT.md
@.planning/phases/01-foundation-and-game-integrity/01-RESEARCH.md
@.planning/phases/01-foundation-and-game-integrity/01-01-SUMMARY.md
@src/App.jsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Normalize persisted settings and summaries before use</name>
  <files>src/App.jsx, src/game/*.js</files>
  <read_first>src/App.jsx, .planning/codebase/CONCERNS.md, .planning/phases/01-foundation-and-game-integrity/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create explicit normalization helpers for persisted settings and summary payloads. Clamp unsupported `difficulty`, `duration`, `playerCount`, `language`, and boolean values to the app's allowed defaults. Ensure recent/best summaries fall back to known-safe numeric and enum values before render-time lookups or result comparisons occur.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - extracted persistence helpers contain logic for allowed settings values
    - `src/App.jsx` calls normalization helpers before using persisted settings or summaries
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Malformed persisted values can no longer directly flow into rendering and summary logic.</done>
</task>

<task type="auto">
  <name>Task 2: Make persistence writes fail safely</name>
  <files>src/App.jsx, src/game/*.js</files>
  <read_first>src/App.jsx, .planning/codebase/CONCERNS.md</read_first>
  <action>Wrap browser storage writes in `try/catch` and return safe failure behavior instead of allowing persistence exceptions to break interactions. Keep the current storage keys and local-only architecture unchanged. If a write fails, gameplay and results flow should continue without throwing from settings or score save paths.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - write helper code contains `try` and `catch` around `localStorage.setItem`
    - `src/App.jsx` uses the safe write path for settings and score persistence
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Persistence failures degrade gracefully instead of crashing the session.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for malformed persistence data</name>
  <files>src/__tests__/*.test.js, src/game/*.js</files>
  <read_first>src/__tests__/*.test.js, src/game/*.js, .planning/phases/01-foundation-and-game-integrity/01-VALIDATION.md</read_first>
  <action>Add tests that feed invalid saved settings and summary shapes into the normalization helpers and verify that the returned objects use safe defaults. Add at least one test proving that a thrown storage write does not escape the safe persistence helper path.</action>
  <verify>npx vitest run src/__tests__</verify>
  <acceptance_criteria>
    - test files contain cases for invalid `playerCount` or `difficulty`
    - test files contain a case for failed persistence writes
    - `npx vitest run src/__tests__` exits with code 0
  </acceptance_criteria>
  <done>Persistence hardening is covered by automated regression checks.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run` succeeds
- [ ] `npm run build` succeeds
- [ ] Invalid persisted values fall back to defaults in tests
</verification>

<success_criteria>

- All tasks completed
- Invalid local data is normalized or discarded safely
- Persistence writes can fail without breaking the session
- Regression tests protect the new persistence contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-game-integrity/01-02-SUMMARY.md`
</output>
