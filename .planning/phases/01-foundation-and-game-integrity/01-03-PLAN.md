---
phase: 01-foundation-and-game-integrity
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/App.jsx
  - src/game/
  - src/__tests__/
autonomous: true
requirements:
  - PRA-03
  - QLT-02
user_setup: []
must_haves:
  truths:
    - "End-of-round unresolved bell windows are reconciled before final summary metrics are persisted."
    - "Round startup, restart, and cleanup paths no longer leave orphaned callbacks that can corrupt state."
    - "The existing playable loop still builds and remains manually playable after timing fixes."
  artifacts:
    - path: "src/App.jsx"
      provides: "Deterministic finish/start/cleanup behavior for the active round loop"
    - path: "src/__tests__/"
      provides: "Regression coverage for final bell reconciliation and restart timing paths"
  key_links:
    - from: "src/App.jsx"
      to: "src/game/"
      proves: "Round-end and bell-state logic use shared helpers rather than ad hoc inline logic"
    - from: "src/__tests__/"
      to: "src/App.jsx"
      proves: "Known timing edge cases are protected by automated regression checks"
---

<objective>
Close the timing and lifecycle integrity gaps, then verify the full phase contract.

Purpose: After the rule engine and persistence are hardened, the remaining Phase 1 work is to make round-end and restart behavior deterministic enough for future feature work.
Output: Fixed lifecycle edge cases, regression coverage for those paths, and final phase-level verification commands.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-game-integrity/01-CONTEXT.md
@.planning/phases/01-foundation-and-game-integrity/01-RESEARCH.md
@.planning/phases/01-foundation-and-game-integrity/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-game-integrity/01-02-SUMMARY.md
@src/App.jsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Reconcile final bell state inside round completion</name>
  <files>src/App.jsx, src/game/*.js</files>
  <read_first>src/App.jsx, .planning/codebase/CONCERNS.md, .planning/ROADMAP.md</read_first>
  <action>Update the finish path so any active unresolved bell window is processed before final summary metrics are derived and persisted. The end-of-round logic must account for missed-hit penalties when the timer expires during a valid bell window, matching the Phase 1 success criteria and the known bug documented in `.planning/codebase/CONCERNS.md`.</action>
  <verify>npx vitest run src/__tests__</verify>
  <acceptance_criteria>
    - `src/App.jsx` or extracted helpers contain explicit reconciliation logic for unresolved bell state before final summary creation
    - automated tests include a round-end valid-bell-window case
    - `npx vitest run src/__tests__` exits with code 0
  </acceptance_criteria>
  <done>Round-end summaries no longer drop the last missed bell window.</done>
</task>

<task type="auto">
  <name>Task 2: Make restart and timer cleanup deterministic</name>
  <files>src/App.jsx, src/game/*.js</files>
  <read_first>src/App.jsx, .planning/codebase/CONCERNS.md, .planning/phases/01-foundation-and-game-integrity/01-CONTEXT.md</read_first>
  <action>Track every startup or loop callback through explicit refs so `stopGameLoops()` or equivalent cleanup can cancel them during restart and unmount. Remove the race where an immediate startup timeout can survive restart/unmount and update stale state after a new round begins.</action>
  <verify>npx vitest run src/__tests__</verify>
  <acceptance_criteria>
    - timer or startup callbacks are stored in clearable refs rather than anonymous unmanaged callbacks
    - regression tests cover restart or cleanup timing behavior
    - `npx vitest run src/__tests__` exits with code 0
  </acceptance_criteria>
  <done>Round lifecycle callbacks can no longer leak across restart or teardown paths.</done>
</task>

<task type="auto">
  <name>Task 3: Run full integrity verification for the phase</name>
  <files>package.json, src/App.jsx, src/__tests__/*.test.js</files>
  <read_first>package.json, .planning/phases/01-foundation-and-game-integrity/01-VALIDATION.md, .planning/ROADMAP.md</read_first>
  <action>Run the full automated verification path for the phase using `npx vitest run && npm run build`. If the repo has a stable dev flow available, do a brief manual check of the current playable loop to confirm ring, miss, and result behavior remain intact aside from the targeted integrity fixes. Record the exact verification commands in the summary output.</action>
  <verify>npx vitest run && npm run build</verify>
  <acceptance_criteria>
    - `npx vitest run && npm run build` exits with code 0
    - summary notes include the final verification commands used
    - no new requirement IDs are introduced beyond `PRA-03` and `QLT-02`
  </acceptance_criteria>
  <done>The phase has a final reproducible verification path and preserved build health.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run && npm run build` succeeds
- [ ] final bell-window regression is covered by tests
- [ ] restart/cleanup behavior is covered by tests or explicit deterministic logic checks
</verification>

<success_criteria>

- All tasks completed
- Final bell and restart integrity gaps are closed
- Full automated verification passes
- The current playable Halligalli loop remains intact after the refactor
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-game-integrity/01-03-SUMMARY.md`
</output>
