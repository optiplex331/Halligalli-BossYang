---
phase: 01-foundation-and-game-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/App.jsx
  - src/game/
  - src/__tests__/
autonomous: true
requirements:
  - PRA-02
  - QLT-01
user_setup: []
must_haves:
  truths:
    - "Core Halligalli rule helpers exist outside React and can be imported by both gameplay and future tutorial flows."
    - "An automated non-watch test command exists and exercises the extracted rule helpers."
  artifacts:
    - path: "src/game/"
      provides: "Shared pure rule modules for totals, ring evaluation, penalties, or summaries"
    - path: "src/__tests__/"
      provides: "Automated regression tests for extracted rule logic"
  key_links:
    - from: "src/App.jsx"
      to: "src/game/"
      proves: "Live gameplay reads extracted shared logic instead of keeping all rule computation inline"
    - from: "package.json"
      to: "src/__tests__/"
      proves: "Repository exposes a runnable automated test command"
---

<objective>
Establish the extracted game-logic foundation and the repo's first automated regression harness.

Purpose: Phase 1 cannot safely fix persistence and timing bugs until the core rule behavior is detached from the giant React component and covered by fast tests.
Output: Shared rule modules, a working test command, and logic regression coverage for core Halligalli evaluation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-game-integrity/01-CONTEXT.md
@.planning/phases/01-foundation-and-game-integrity/01-RESEARCH.md
@src/App.jsx
@package.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Install and wire the first automated test harness</name>
  <files>package.json, package-lock.json, vitest.config.js</files>
  <read_first>package.json, .planning/phases/01-foundation-and-game-integrity/01-RESEARCH.md, .planning/phases/01-foundation-and-game-integrity/01-VALIDATION.md</read_first>
  <action>Add `vitest` as a dev dependency, create a stable non-watch test command in `package.json`, and add `vitest.config.js` only if Vite config reuse is insufficient. The repository must expose a command that runs once and exits cleanly for automated verification. Keep the existing `dev`, `build`, and `preview` scripts unchanged.</action>
  <verify>npm install && npx vitest run</verify>
  <acceptance_criteria>
    - `package.json` contains `"vitest"` in `devDependencies`
    - `package.json` contains a script line matching `"test":`
    - `npx vitest run` exits with code 0 after test files are added
  </acceptance_criteria>
  <done>Vitest is installed and runnable in non-watch mode from the repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Extract shared game-rule helpers from App</name>
  <files>src/App.jsx, src/game/*.js</files>
  <read_first>src/App.jsx, .planning/codebase/ARCHITECTURE.md, .planning/codebase/CONCERNS.md, .planning/phases/01-foundation-and-game-integrity/01-CONTEXT.md</read_first>
  <action>Create one or more pure modules under `src/game/` that hold deterministic Halligalli logic currently embedded in `src/App.jsx`. At minimum extract the logic for visible totals, bell evaluation inputs, penalty card movement, and end-of-round summary calculation. Update `src/App.jsx` to import and use those helpers without changing intended gameplay semantics other than explicit integrity fixes deferred to later plans.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `src/game/` exists with exported helper functions for game-rule logic
    - `src/App.jsx` contains at least one import from `./game/` or a nested `src/game/` path
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>The core rule logic is no longer exclusively defined inline inside `src/App.jsx`.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for extracted rule behavior</name>
  <files>src/__tests__/*.test.js, src/game/*.js</files>
  <read_first>src/game/*.js, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-and-game-integrity/01-VALIDATION.md</read_first>
  <action>Add automated tests for extracted rule helpers covering visible fruit totals, valid ring detection inputs, wrong-ring penalty handling, and summary math. Use concrete fixture-style data that mirrors the current player/card object shapes from `src/App.jsx` so the tests protect the live product contract rather than a simplified toy model.</action>
  <verify>npx vitest run src/__tests__</verify>
  <acceptance_criteria>
    - `src/__tests__/` contains at least one `.test.` file
    - test files contain assertions for totals, penalties, and summary behavior
    - `npx vitest run src/__tests__` exits with code 0
  </acceptance_criteria>
  <done>Rule-engine behavior has automated regression coverage.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run` succeeds
- [ ] `npm run build` succeeds
- [ ] `src/App.jsx` uses extracted helpers from `src/game/`
</verification>

<success_criteria>

- All tasks completed
- Automated test command exists and passes
- Extracted rule helpers protect shared behavior for future phases
- No build regressions introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-game-integrity/01-01-SUMMARY.md`
</output>
