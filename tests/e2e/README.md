# End-to-End Journeys

This directory is reserved for browser journeys that cross the deployed Web and
API seam. Web-only behavior tests stay with `apps/web`; API module tests stay
with `apps/api`. The first cross-application journey arrives with authenticated
room entry.
