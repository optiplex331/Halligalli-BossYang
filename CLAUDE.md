# Claude Agent Entry

Use `AGENTS.md` as the source of truth for this Child Repo's agent instructions.

Keep this file as a thin compatibility entry so Claude-specific agents do not drift from the shared repository rules.
